# Finedge Automation

This project automates the task of fetching a webhook, generating a SQL query to find the highest paid employee per department, and submitting the result to the Finedge API.

## Requirements

- Python 3
- `requests` library

## Installation

```bash
pip install -r requirements.txt
```

## Running the Application

```bash
python main.py
```
